# Rah_Negar final defect register

Confirmed defects are separated from unproven risks. The original rows below remain the finding baseline; the Batch 1 status ledger records evidence-backed closure without changing unrelated scope.

## Batch 1 and Batch 2 status ledger (2026-09-07)

| ID | Current status | Evidence |
|---|---|---|
| D-01 | RESOLVED | Seeder service and Records entry points removed; static production-assembly regression test passes. |
| D-02 | RESOLVED | Public runtime calculator validates complete canonical chains and uses the state-machine projection; invalid-chain regression test passes. |
| D-03 | RESOLVED | Legacy deterministic recovery service removed; managed recovery expiry and durable recovery-required blocking are covered. |
| D-04 | RESOLVED | Backup encryption is versioned AES-GCM with DPAPI-protected operator key custody and tamper tests. |
| D-05 | RESOLVED | Import performs integrity/FK/schema identity checks, verified rollback capture, staged atomic replacement, post-check, and fail-closed recovery marking. |
| D-06 | RESOLVED | Factory reset requires ManagementCredential proof, a separate compatible verified backup, integrity validation, and sidecar cleanup. |
| D-08 | RESOLVED FOR SAFETY | Ordinary-password confirmation and unsafe UI calls were removed; protected service entry points require canonical proof and fail closed. |
| D-07 | RESOLVED | Canonical ProgramData root, WAL-aware retained-source migration, Inno Setup installer, payload audit, and lifecycle preservation harness pass. |
| D-09 | RESOLVED | QualificationTool build boundary is explicit; clean Phase 9.7 wrapper run passes with isolated artifacts. |
| D-10 | OPEN | Common UI RTL/control/message policy and automated DPI invariants were added, but native desktop visual/keyboard acceptance remains unavailable. |

The production authority decision remains Legacy authoritative / Target non-authoritative / target routing disabled / Production Activation and Cutover unauthorized.

| ID | Severity | File / class / method | Evidence and failure scenario | Required correction | Test required |
|---|---|---|---|---|---|
| D-01 | CRITICAL | `UI/Forms/FrmRecords.cs`, `button1_Click`, `button11_Click` | Normal Records UI invokes `TestDataSeederService` with fixed template and month/year values. Synthetic data can enter or overwrite operational history. | Remove handlers/controls from production; isolate seeders to a test-only project and fail build/package checks if exposed. | UI composition absence test; disposable end-to-end data-integrity test. |
| D-02 | CRITICAL | `Services/Reports/EventRuntimeCalculationService.Calculate` | Public method dispatches to `CalculateLegacyCore`; approved Event semantics are not authoritative. Reports may change for invalid transitions or incomplete history. | Route active reporting through the validated complete-chain state machine; reject invalid transitions below UI. | Golden-chain tests for all states, cross-day history, Running+OH, stopped ESD, and report equivalence. |
| D-03 | CRITICAL | `Services/RecoveryService.GetRecoverySecretKey` / `GenerateRecoveryCode` | Recovery secret is assembled in source (`Rn@` + `Recovery` + `Key` + `_2026`); anyone with the binary/source can reproduce codes. Requests also lack expiry. | Remove legacy deterministic secret recovery; compose a managed recovery boundary with operator-held material, expiry, one-time use, audit, and rotation. | Black-box recovery attack test, expiry/replay/rotation tests, no-secret static scan. |
| D-04 | CRITICAL | `Services/BackupEncryptionService` | Hard-coded internal backup key; AES-CBC has no authenticated integrity tag. A backup can be decrypted by anyone who extracts the binary key and tampering is not authenticated. | Use an approved key-custody design and authenticated encryption; version the format and reject tampered files before restore. | Key-custody, tamper, wrong-key, version, and restore tests. |
| D-05 | HIGH | `Services/DatabaseMaintenanceService.ImportDatabase` | Declares `safetyBackupPath` but never creates it; overwrites the live DB with `File.Copy`; no atomic replace or post-replacement integrity validation. Failure during replacement can lose/corrupt data. | Take verified safety backup, stage/validate, atomically replace, handle WAL/SHM, validate after replacement, and retain rollback receipt. | Failure injection at every step, active-DB backup, restart/power-loss simulation, rollback test. |
| D-06 | HIGH | `Services/DatabaseMaintenanceService.FactoryReset` | Deletes only `db.sys`; WAL/SHM sidecars may remain and no verified-backup gate is enforced in the service. | Make reset an authorized, audited, sidecar-aware operation with verified backup and explicit data-path confirmation. | Sidecar/reset/restart/cancel/double-click/permission tests. |
| D-07 | HIGH | `Data/SqliteDatabaseHelper`, `Utils/ErrorLogger` | Mutable DB/logs are rooted at `AppDomain.CurrentDomain.BaseDirectory`; installing under Program Files will make first run/write/diagnostics permission-sensitive. | Move mutable data/logs to a versioned per-machine or per-user operator data root; migrate existing data safely. | Standard-user install, read-only install, upgrade/reinstall, migration, and data-preservation tests. |
| D-08 | HIGH | `FrmSettings` protected actions | Ordinary login password confirmation is used for backup/import/repair/reset while the target design requires independent ManagementCredential authorization. | Implement and compose the target authorization boundary for every protected action; fail closed on unavailable audit/credential. | Action/scope/version/expiry/rollback/concurrency and UI integration tests. |
| D-09 | HIGH | `QualificationTool` project / nested `Phase98Probe` | `run-phase9.7-final-qualification.ps1` fails before qualification because SDK default compile items include both top-level `Program.cs` files, producing CS8802 and duplicate assembly attributes. | Exclude nested probe sources or make the probe a separate project; make the qualification runner build gate explicit. | Run all relevant qualification scripts from a clean checkout. |
| D-10 | HIGH | Whole UI acceptance evidence | Real WinForms inspection at 1920×1080/100/125/150% was unavailable in this audit; prior docs explicitly keep manual UI acceptance open. | Execute and retain independent desktop evidence before product acceptance. | Form-by-form visual, keyboard, RTL, DPI, accessibility, error, and destructive-action checklist. |
| D-11 | MEDIUM | `FrmReportCenter`, grid configurators; `FrmRecords.ApplyGridProfileToDataGridView` | Grid columns/rows were cleared and recreated; no cache/reuse or performance baseline. | RESOLVED in Batch 3 with `DataGridViewDefinitionCache` and controlled probe evidence. | Batch3CompletionTests; batch3-performance-result.json. |
| D-12 | MEDIUM | `FrmReportCenter` summary/notification paths | Most-frequent summary was computed but not displayed. | RESOLVED in Batch 3; summary is rendered and finalization messages use the shared UI service. | Batch3CompletionTests; native checklist for visual confirmation. |
| D-13 | MEDIUM | `AppSettingsService.GetDataStartDate` | Debug `MessageBox.Show(... "NULL")` exposed internal state on invalid settings. | RESOLVED in Batch 3; malformed values fall back to a user-actionable error. | Batch3CompletionTests; source gate. |
| D-14 | LOW | `FrmRecovery.Designer` dead handler; stale TODOs | Unwired handler threw `NotImplementedException`; stale TODOs suggested unfinished paths. | RESOLVED in Batch 3; handler and reviewed stale TODOs removed. | Batch3CompletionTests; static source gate. |

Current unresolved CRITICAL/HIGH item is D-10/H-05: native desktop visual/keyboard/DPI acceptance. D-11 through D-14 are closed by Batch 3 evidence. Target composition is intentionally inactive by owner decision; its disabled write boundary is tested and does not authorize activation or cutover.
